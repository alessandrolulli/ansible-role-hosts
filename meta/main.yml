---
galaxy_info:
    author: Alessandro Lulli
    description: Set /etc/hostname and /etc/hosts
    min_ansible_version: 2.0
    platforms:
        - name: EL
          versions:
              - all
        - name: Ubuntu
          versions:
              - all
        - name: Debian
          versions:
              - all
    categories:
        - networking
        - system

dependencies: []

